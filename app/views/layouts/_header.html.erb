<% unless suppress_heading? %>
  <div class="container">
    <div class="page-header">
      <h1><%= heading %>
      <% if tagline %>
        <small><%= tagline %></small>
      <% end %>
      </h1>
    </div>  
  </div>
<% end %>